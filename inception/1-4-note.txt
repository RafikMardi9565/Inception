Additions to 1-4-pid1-foreground.md

- New section 7.1 "Tuning the Stop Sequence: STOPSIGNAL and stop_grace_period" (custom signal, grace period)
- tini section: built-in alternative (docker run --init / init: true in compose)
- Check Your Understanding: new question 13 (in-namespace SIGKILL vs docker kill; grace period tuning)

Corrections to 1-4-pid1-foreground.md

- Fixed PID 1 table row: kernel panic only applies to the host's global init; in-namespace signals (incl. SIGKILL) without handlers are ignored; host-side SIGKILL kills the container
- Added clarification paragraph in 1.2: docker exec kill -9 1 does nothing (same-namespace SIGKILL ignored); docker kill works
- Fixed verification test: replaced `docker exec nginx kill -9 1` with `docker kill -s KILL nginx`
- Fixed exit-code explanation (0 = graceful SIGTERM shutdown; 137 = SIGKILL, 128+9)
