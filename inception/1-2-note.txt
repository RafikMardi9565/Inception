Additions to 1-2-docker-architecture.md

- containerd-shim added to the architecture diagram and flow (one per container; stdio, exit status, survives daemon restarts)
- New section 1.5 "containerd-shim" (renumbered Complete Flow to 1.6)
- containerd bullet updated: spawns shim, delegates process creation to runc through it
- runc section: shim reparents the container process after runc exits
- Dockerfile instruction table: HEALTHCHECK and STOPSIGNAL rows
- New section 4.7 "Multi-Stage Builds" (builder stage + COPY --from example)
- BuildKit note in docker build step-by-step (DOCKER_BUILDKIT default in Docker 23+)
- Check Your Understanding: 2 new questions (shim, multi-stage builds)

Corrections to 1-2-docker-architecture.md

- Fixed identical BAD/GOOD cache-busting examples (6.2): BAD now COPY . . before pip install
- Fixed lifecycle diagram state names: Container → Created, Run → Running
